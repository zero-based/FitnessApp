<Page x:Class="FitnessApp.UserMainWindowPages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      mc:Ignorable="d">

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden" Padding="2.55">
        <Grid Margin="2.5">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="25"/>
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            
            <!--Left cards grid-->
            <Grid Grid.Column="0">

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <!--Left Upper Card-->
                <Grid Grid.Row="0">
                    <materialDesign:Card 
                        Width="485" 
                        Background="{StaticResource MaterialDesignBackground}">
                        <Grid>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="15"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="15"/>
                            </Grid.RowDefinitions>
                            
                            

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                            </Grid.ColumnDefinitions>

                            <!--Card Header-->
                            <TextBlock Grid.Row="1"
                                       Grid.Column="1"
                                       FontFamily="{StaticResource ProductSans}"
                                       FontWeight="Bold" 
                                       FontSize="18"
                                       Foreground="Black"
                                       Text="Weight"/>

                            <Grid Grid.Row="3" Grid.Column="1">
                                <lvc:CartesianChart Series="{Binding SeriesCollection}" LegendLocation="Right" Width="auto" Height="200">
                                    <lvc:CartesianChart.AxisY>
                                        <lvc:Axis Title="Sales" LabelFormatter="{Binding YFormatter}"></lvc:Axis>
                                    </lvc:CartesianChart.AxisY>
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Title="Month" Labels="{Binding Labels}"></lvc:Axis>
                                    </lvc:CartesianChart.AxisX>
                                </lvc:CartesianChart>
                            </Grid>


                        </Grid>
                    </materialDesign:Card>
                </Grid>

                <!--Left Lower Card-->
                <Grid Grid.Row="2">
                    <materialDesign:Card 
                        Width="485" 
                        Background="{StaticResource MaterialDesignBackground}">
                        <Grid>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="15"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="15"/>
                            </Grid.RowDefinitions>
                            
                            

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                            </Grid.ColumnDefinitions>



                        </Grid>
                    </materialDesign:Card>
                </Grid>

            </Grid>

            <!--Right cards grid-->
            <Grid Grid.Column="2">

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <!--Right Upper Card-->
                <Grid Grid.Row="0">
                    <materialDesign:Card Width="485" 
                        Background="{StaticResource MaterialDesignBackground}">
                        <Grid>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="15"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="15"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                            </Grid.ColumnDefinitions>

                        </Grid>
                    </materialDesign:Card>
                </Grid>

                <!--Right Lower Card-->
                <Grid Grid.Row="2">
                    <materialDesign:Card Width="485" 
                        Background="{StaticResource MaterialDesignBackground}">
                        <Grid>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="15"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="15"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                            </Grid.ColumnDefinitions>

                        </Grid>
                    </materialDesign:Card>
                </Grid>

            </Grid>

            <!--Floating PopUp Button-->
            <materialDesign:PopupBox Style="{StaticResource MaterialDesignMultiFloatingActionPopupBox}"
                                     ToolTipService.Placement="Right"
                                     HorizontalAlignment="Right"
                                     VerticalAlignment="Bottom"
                                     Grid.Column="2"
                                     Margin="5">
                
                <StackPanel>
                    
                    <Button>
                        
                    </Button>
                    
                    <Button>
                        
                    </Button>

                    <Button>

                    </Button>
                    
                </StackPanel>
                
            </materialDesign:PopupBox>
            
        </Grid>
        
    </ScrollViewer>
    
</Page>
